#!/bin/bash

function dclocal(){
    docker-compose -f docker-compose.yml -f docker/docker-compose-dev.yml $@
}

dclocal up -d --force-recreate

docker-console cache:clear

dclocal logs -f webapp import
